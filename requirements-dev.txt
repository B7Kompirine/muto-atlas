# Python packages the repository checks need: python -m pip install -r requirements-dev.txt
# (scripts/check_repo.py runs every script's --help, and some scripts import these at start-up.)
numpy
Pillow
mcp>=1.28
